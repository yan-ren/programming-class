# a = 2
# b = 3
# print(a)
# print(b)
# # we need to store a's old value before it's changed
# tmp = a
# a = b # a gets b's value
# b = tmp # a's value is changed, b cannot get a's old value
# print(a)
# print(b)
#
# x = 1
# y = str(x)

# operator that generates a boolean == != > < <= >=
# comparison operator compares values, tell you the result in boolean

# boolean operator: and or not, directly used for boolean

# print(not (1 == 1 and 0 != 1) )

s = 'Arthur'
# for each character in the string, they automatically get an ID number
# have six letters, which needs six ids, the id starts from 0
# so the largest id is 5
s = 'abcde'
print(s[0])
print(s[2])
print(s[4])
# what's the relation between number of characters in string
# and the largest index
print(s[-1])
print(s[0:2])
print(s[1:3])
