# write a function that takes two lists as input, and merge them return a single list
# def merge(l1, l2):
#     result = []
#     for value in l1:
#         if value not in result:
#             result.append(value)
#     for value in l2:
#         if value not in result:
#             result.append(value)
#
#     return result
#
# print(merge([1, 2, 3], [2, 4]))

# write a function that takes a list as input, return how many positive number in the list
# def count_positive(numbers):
#     count = 0
#     for num in numbers:
#         if num > 0:
#             count += 1
#
#     return count
#
# print(count_positive([1, 2, -1, -2, 3]))

# write a function that takes a list as input, return how many even number in the list

# exercise: write a function that takes a list as input, return a new list with only even numbers