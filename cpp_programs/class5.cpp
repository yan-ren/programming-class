#include <iostream>
#include <string>

using namespace std;

int main()
{
    // double total = 0.0;
    // int i = 1;
    // while (i <= 50)
    // {
    //     total = total + (i / (51.0 - i));
    //     i++;
    // }

    // cout << total << endl;

    for (int i = 0; i < 10; i++)
    {
        cout << i << endl;
    }

    /*
    Write a program that calculates the factorial of a given number N. The factorial of N (denoted as N!) is the product of all positive integers less than or equal to N.

    Write a program that calculates the sum of all odd numbers between 1 and 100.
    */
    cout << "Result: " << 5 << endl;
    return 0;
}
